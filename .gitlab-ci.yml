image: ruby:2.3

build:
  stage: build
  script:
  - apt-get update
  - apt-get install -y node
  - apt-get install -y npm
  - npm install
  - npm install -g grunt
  - npm install -g grunt-cli
  - npm install -g bower
  - bower install --allow-root
  - gem install jekyll
  - grunt
